<div class="container">
	<div class="row">
		<% if flash[:notice] %>
	      <div class="flash">
	        <%= flash[:notice] %>
	      </div>
	    <% end %>

	    <div class="col-xs-3" >
		<%= render 'users/list', user: @book.user, book: @bookn %>
		</div>

			<div class="col-xs-9">
				<div class="books-h2">
					<h2>Book detail</h2>
				</div>

				<div class="books-index">
					<table class="table table-striped">
						<thead>
							<tr>
								<th></th>
								<th></th>
								<th></th>
								<th></th>
								<th></th>
							</tr>
						</thead>
						<tbody>
							<tr>
								<td><%= link_to user_path(@book.user) do %>
									<%= attachment_image_tag @book.user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "40x40" %><br>
									<%= @book.user.name %>
									<% end %>
								</td>

								<td><%= link_to @book.title, book_path(@book) %></td>
								<td><%= @book.body %></td>

								<td><% if @book.user.id == current_user.id %>
									<%= link_to 'Edit',edit_book_path(@book) %>
									<% end %>
								</td>

								<td><% if @book.user.id == current_user.id %>
									<%= link_to "Destroy", book_path(@book), method: :delete %>
									<% end %>
								</td>

							</tr>
						</tbody>
					</table>
				</div>

			</div>
	</div>

</div>